# PHP CRUD Project

This project is a simple PHP CRUD (Create, Read, Update, Delete) application built using a college-provided template. It demonstrates basic database operations in PHP, including adding, viewing, editing, and deleting records.
